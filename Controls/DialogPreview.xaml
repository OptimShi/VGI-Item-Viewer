<Window x:Class="VGI_Item_Viewer.Controls.DialogPreview"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        WindowStartupLocation="CenterOwner" 
        FocusManager.FocusedElement="{Binding ElementName=btnOK}"
        ShowInTaskbar="False"
        Title="Item Preview" Height="370" Width="330" ResizeMode="CanResizeWithGrip" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" HorizontalAlignment="Stretch">
            <TextBlock x:Name="txtItemDescription" HorizontalAlignment="Stretch" TextWrapping="Wrap" Margin="5"/>
        </ScrollViewer>
        <!-- <TextBox Grid.Row="0" x:Name="txtItemDescription" TextWrapping="Wrap" Text="TextBox" HorizontalAlignment="Left" VerticalAlignment="Top" VerticalScrollBarVisibility="Auto"/>
        -->
        <Button Grid.Row="1" x:Name="btnOK" Content="Close"  HorizontalAlignment="Center" VerticalAlignment="Center" Width="48" Click="btnOK_Click"/>
    </Grid>

</Window>
